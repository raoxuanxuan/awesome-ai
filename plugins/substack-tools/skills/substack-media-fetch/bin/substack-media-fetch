#!/usr/bin/env bash
set -euo pipefail

SCRIPT_PATH="${BASH_SOURCE[0]}"
BIN_DIR="${SCRIPT_PATH%/*}"
SKILL_DIR="$(cd "${BIN_DIR}/.." && pwd)"

exec python3 "${SKILL_DIR}/scripts/substack_media_fetch.py" "$@"
